version: '3'
services:
    rasa:
      container_name: "rasa_server_cae"
      user: root
      build: 
        context:  .
      volumes:
      - "./:/app"
      ports: 
        - "5003:5005"
    action_server:
      container_name: "action_server_cae"
      build: 
        context: actions
      volumes:
        - ./actions:/app/actions
        - ./data:/app/data
      ports:
        - 5053:5055
